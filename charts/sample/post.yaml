method: POST
endpoint: /api/users
headers:
    Content-Type: application/json
payload: >
  {
    "name": "{{ leader.name | default("morpheus", true) }}",
    "job": "leader"
  }
cleanup:
  - user_id
capture:
  - value: id
    dest: user_id
